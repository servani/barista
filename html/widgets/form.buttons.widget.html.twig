<input type="submit" class="button radius submit" value="{% if edit %}Guardar{% else %}Crear{% endif %}" />
{% if edit %}
	<a data-value="{{ deletevalue }}" href="{{ url('admin/delete/' ~ entityName ~ '/' ~ entity.id ) }}" title="Borrar" class="button alert radius delete delen">Borrar</a>
{% endif %}